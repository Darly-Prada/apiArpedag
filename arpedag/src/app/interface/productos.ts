export interface Productos {
    _id?:string;
    nombre:string,                         // nombre del producto
    tipo:string,                       // tipo de material: pastilla, barra, burbuja 
    codigo:string,                          // cambiar por un codigo que vaya aumentando 1
    disponible:boolean,

// agregar codigo , arreglar el backend 

//codigoConsecutivo = 0; const productos = length= 0;   
//productos.length + 1  
}
